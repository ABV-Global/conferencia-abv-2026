{
  "name": "conferencia-abv-2026",
  "version": "1.0.0",
  "private": true,
  "description": "CONFERÊNCIA ABV 2026 — Da confissão à obediência. A cultura da unidade.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/serve.mjs",
    "check": "node --check scripts/build.mjs && node --check scripts/serve.mjs && node --check registration.mjs && node --test scripts/registration.test.mjs"
  }
}
